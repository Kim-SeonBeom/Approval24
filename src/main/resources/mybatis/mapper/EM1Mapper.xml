<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.approval24.dao.EM1DAO">

	<!-- 민원 유무 조회 -->
	<select id="existByComplainId" resultType="int"
		parameterType="long">
		select count(*)
		from t4000_em1
		where complain_id
		=
		#{complainId}
	</select>

	<!-- 민원상세 조회 -->
	<select id="findByComplainId" resultType="EM1DTO"
		parameterType="long">
		select *
		from t4000_em1
		where complain_id
		= #{complainId}
	</select>

	<!-- 민원 등록 -->
	<insert id="insertInfo" parameterType="EM1DTO">
		insert into
		t4000_em1(complain_id)
		values(#{complainId})
	</insert>

	<!-- 민원 수정 -->
	<update id="updateInfo" parameterType="EM1DTO">
		UPDATE t4000_em1
		SET
		BIZ_OWNER_NM = #{bizOwnerNm},
		INDUSTRY_TYPE = #{industryType},
		EMPLOYMENT_DT =#{employmentDt},
		BIZ_POST = #{bizPost},
		BIZ_ADDR = #{bizAddr},
		BIZ_ADDR_DETAIL = #{bizAddrDetail},
		TRAIN_COURSE_NM = #{trainCourseNm},
		TRAIN_INSTITUTE_NM = #{trainInstituteNm},
		TRAIN_START_DT =#{trainStartDt},
		TRAIN_END_DT = #{trainEndDt},
		ACCOUNT_HOLDER_NM = #{accountHolderNm},
		BANK_NM = #{bankNm},
		ACCOUNT_NO = #{accountNo},
		UPDATE_DT = SYSDATE
		WHERE COMPLAIN_ID =
		#{complainId}
	</update>


</mapper>
 