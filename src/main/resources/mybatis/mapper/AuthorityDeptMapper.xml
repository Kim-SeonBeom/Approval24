<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.approval24.dao.AuthorityDeptDAO">

    <select id="getDeptsByAuthorityId" parameterType="long" resultType="com.example.approval24.domain.DeptDTO">
        SELECT
            D.DEPT_ID,
            D.DEPT_NAME
        FROM T1000_DEPARTMENT D
        JOIN T3000_AUTHORITY_DEPT AD ON D.DEPT_ID = AD.DEPT_ID
        WHERE AD.AUTHORITY_ID = #{authorityId}
          AND AD.DEL_YN = 'N' 
          AND D.DEL_YN = 'N'
        ORDER BY D.DEPT_ID ASC
    </select>

    <insert id="insertAuthorityDept" parameterType="com.example.approval24.domain.AuthorityDeptDTO">
        INSERT INTO T3000_AUTHORITY_DEPT (
            DEPT_ID, AUTHORITY_ID, CREATE_ID
        ) VALUES (
            #{deptId},
            #{authorityId},
            #{createId}
        )
    </insert>

    <select id="selectAuthorityDept" resultType="com.example.approval24.domain.AuthorityDeptDTO">
        SELECT 
            DEPT_ID, AUTHORITY_ID, CREATE_DT, UPDATE_DT, CREATE_ID, UPDATE_ID, DEL_YN
        FROM T3000_AUTHORITY_DEPT
        WHERE AUTHORITY_ID = #{authorityId}
          AND DEPT_ID = #{deptId} 
    </select>
    
    <update id="updateAuthorityDept" parameterType="com.example.approval24.domain.AuthorityDeptDTO">
        UPDATE T3000_AUTHORITY_DEPT
        SET
            UPDATE_DT = SYSDATE,
            UPDATE_ID = #{updateId},
            DEL_YN = #{delYn}
        WHERE AUTHORITY_ID = #{authorityId}
          AND DEPT_ID = #{deptId}
    </update>
</mapper>