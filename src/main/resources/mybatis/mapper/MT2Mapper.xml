<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.approval24.dao.MT2DAO">

	<!-- 민원 유무 조회 -->
	<select id="existByComplainId" resultType="int"
		parameterType="long">
		select count(*)
		from t4000_mt2
		where complain_id
		=
		#{complainId}
	</select>

	<!-- 민원상세 조회 -->
	<select id="findByComplainId" resultType="MT2DTO"
		parameterType="long">
		select *
		from t4000_mt2
		where complain_id
		= #{complainId}
	</select>

	<!-- 민원 등록 -->
	<insert id="insertInfo" parameterType="MT2DTO">
		insert into
		t4000_mt2(complain_id)
		values(#{complainId})
	</insert>

	<!-- 민원 수정 -->
	<update id="updateInfo" parameterType="MT2DTO">
		UPDATE T4000_MT2
		SET 
		BIRTHDATE = #{birthdate}
		, BABY_RESI_NO = #{babyResiNo}
		, COHABIT_YN = #{cohabitYn}
		, RELATIONSHIP = #{relationship}
		, BIZ_OWNER_NM = #{bizOwnerNm}
		, BIZ_POST = #{bizPost}
		, BIZ_ADDR = #{bizAddr}
		, BIZ_ADDR_DETAIL= #{bizAddrDetail}
		, EMP_INSUR_MNG_NO = #{empInsurMngNo}
		, BIZ_REG_NO = #{bizRegNo}
		, CORP_REG_NO = #{corpRegNo}
		, SELF_EMP_INSUR_YN = #{selfEmpInsurYn}
		, BENIFIT_TYPE = #{benefitType}
		, BANK_NM = #{bankNm}
		, ACCOUNT_NO = #{accountNo}
		, ACCOUNT_HOLDER_NM = #{accountHolderNm}
		, INCOME_ACT = #{incomeAct}
		, APPL_PERIOD_EXT_REASON = #{applPeriodExtReason}
		, UPDATE_DT = SYSDATE
		WHERE COMPLAIN_ID = #{complainId}
	</update>


</mapper>
 